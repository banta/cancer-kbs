<%= form_for(@cancer_symptom) do |f| %>
  <% if @cancer_symptom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cancer_symptom.errors.count, "error") %> prohibited this cancer_symptom from being saved:</h2>

      <ul>
      <% @cancer_symptom.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :desc %><br />
    <%= f.text_area :desc %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
